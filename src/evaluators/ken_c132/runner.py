def run(paper_content, gen_outputs_content):

    # Your core code which runs some algo
    # on the paper_content and gen_outputs_content
    # and produces required
    # outputs. 

    outputs = {
        "report": f"{paper_content} is good!"
    }
    
    return outputs